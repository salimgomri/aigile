# Copier sur le serveur: cp deploy/env.aigile.example /var/www/aigile/.env.local
# Puis remplir les valeurs sensibles (secrets, clés API)

NEXT_PUBLIC_APP_URL=https://aigile.lu
BETTER_AUTH_URL=https://aigile.lu

# Les autres variables (DATABASE_URL, GOOGLE_*, etc.) doivent être copiées depuis .env.local local
